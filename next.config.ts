import type { NextConfig } from "next";
module.exports = {
  images: {
    domains: ['image01-in.oneplus.net','i.pinimg.com'], // ✅ Add the allowed domain here
  },
};
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
